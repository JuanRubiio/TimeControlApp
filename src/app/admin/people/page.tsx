import { People } from '@/admin/components'; import { requirePageSession } from '@/auth/page-guard'; import { productModuleStatus } from '@/modules/service'; import { redirect } from 'next/navigation'; export default async function PeoplePage(){await requirePageSession('/admin/people');if(await productModuleStatus('people_management')!=='active')redirect('/admin/configuration?module=people_management');return <People/>;}
