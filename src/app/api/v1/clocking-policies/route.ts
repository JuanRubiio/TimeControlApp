import { NextRequest } from 'next/server'; import { authenticated,data,invalid } from '@/company-people/http'; import { createPolicy,listPolicies } from '@/clocking-policy/service'; import { policyInput } from '@/clocking-policy/validation';
export async function GET(request:NextRequest){const access=await authenticated(request,'clocking-policy.write');if('response'in access)return access.response;return data(await listPolicies(),access.cid);}
export async function POST(request:NextRequest){const access=await authenticated(request,'clocking-policy.write');if('response'in access)return access.response;const parsed=policyInput.safeParse(await request.json().catch(()=>null));if(!parsed.success)return invalid(access.cid);try{const policy=await createPolicy(parsed.data,access.actor.id,access.cid);return policy?data(policy,access.cid,201):invalid(access.cid);}catch{return data({code:'CONFLICT'},access.cid,409);}}
