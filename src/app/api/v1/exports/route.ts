import { NextRequest } from 'next/server';
import { authenticated } from '@/company-people/http'; import { error } from '@/auth/http'; import { exportInput } from '@/exports/http'; import { appendAudit } from '@/audit/audit-writer';
export async function POST(request:NextRequest){const auth=await authenticated(request,'export.create:scope');if('response'in auth)return auth.response;const body=exportInput.safeParse(await request.json().catch(()=>null));if(!body.success)return error('VALIDATION_FAILED','Los parámetros de exportación no son válidos.',422,auth.cid);await appendAudit({actorType:'user',actorId:auth.actor.id,action:'export.requested',resourceType:'export',result:'denied',correlationId:auth.cid,changes:{format:body.data.format,reason:'S15_NOT_CLOSED'}});return error('EXPORTS_LOCKED','Exportaciones permanece bloqueado hasta el cierre verificable de S15.',409,auth.cid);}
