import { NextRequest } from 'next/server'; import { authenticated,data,invalid } from '@/company-people/http'; import { createZone,listZones } from '@/clocking-policy/service'; import { zoneInput } from '@/clocking-policy/validation';
export async function GET(request:NextRequest){const access=await authenticated(request,'clocking-policy.write');if('response'in access)return access.response;return data(await listZones(),access.cid);}
export async function POST(request:NextRequest){const access=await authenticated(request,'clocking-policy.write');if('response'in access)return access.response;const parsed=zoneInput.safeParse(await request.json().catch(()=>null));if(!parsed.success)return invalid(access.cid);const zone=await createZone(parsed.data,access.actor.id,access.cid);return zone?data(zone,access.cid,201):invalid(access.cid);}
