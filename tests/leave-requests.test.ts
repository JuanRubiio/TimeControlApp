import { describe,expect,it } from 'vitest'; import { readFileSync } from 'node:fs'; import { leaveDecisionInput,leaveRequestInput } from '../src/leave-requests/validation';
describe('S24 solicitudes operativas sintéticas',()=>{it('acepta categorías cerradas, comentario opcional limitado e intervalo de calendario válido',()=>{expect(leaveRequestInput.safeParse({fromDate:'2026-09-14',toDate:'2026-09-15',category:'personal_management',comment:'Necesito reorganizar mi disponibilidad.'}).success).toBe(true);expect(leaveRequestInput.safeParse({fromDate:'2026-09-15',toDate:'2026-09-14',category:'general_request'}).success).toBe(false);expect(leaveRequestInput.safeParse({fromDate:'2026-09-14',toDate:'2026-09-15',category:'medical'}).success).toBe(false);expect(leaveRequestInput.safeParse({fromDate:'2026-09-14',toDate:'2026-09-15',category:'general_request',reason:'dato sensible'}).success).toBe(false);expect(leaveRequestInput.safeParse({fromDate:'2026-09-14',toDate:'2026-09-15',category:'general_request',comment:'x'.repeat(501)}).success).toBe(false);});it('limita decisión a una opción humana sin motivo libre',()=>{expect(leaveDecisionInput.safeParse({decision:'approved'}).success).toBe(true);expect(leaveDecisionInput.safeParse({decision:'rejected',reason:'dato sensible'}).success).toBe(false);});it('mantiene evidencia y permisos aislados del cálculo',()=>{const sql=readFileSync('migrations/s024_202609141500_leave_requests.sql','utf8');const expanded=readFileSync('migrations/s026_202609141650_leave_request_categories_and_comment.sql','utf8');expect(sql).toContain("category='general_request'");expect(sql).toContain('leave-request.decide:scope');expect(expanded).toContain('request_comment');expect(sql).not.toContain('time_events');expect(sql).not.toContain('daily_calculations');});});
