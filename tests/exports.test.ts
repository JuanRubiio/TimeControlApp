import { describe, expect, it } from 'vitest';
import { integrityHash, renderCsv, renderPdf } from '@/exports/service';
import type { ExportSnapshot } from '@/exports/contracts';

const snapshot:ExportSnapshot={company:'Empresa Sintética',scope:'employee',from:'2026-09-01',to:'2026-09-01',records:[{employeeId:'00000000-0000-0000-0000-000000000001',employee:'Ana Prueba',siteId:'00000000-0000-0000-0000-000000000002',site:'Centro Norte',laborDate:'2026-09-01',timeZone:'Europe/Madrid',events:[{id:'e1',type:'clock_in',occurredAt:'2026-09-01T07:00:00.000Z'},{id:'e2',type:'break_start',occurredAt:'2026-09-01T10:00:00.000Z'},{id:'e3',type:'break_end',occurredAt:'2026-09-01T10:15:00.000Z'},{id:'e4',type:'clock_out',occurredAt:'2026-09-01T15:00:00.000Z'}],calculation:{id:'c1',revision:1,effectiveMinutes:465,expectedMinutes:480,breakMinutes:15,incidents:[],ruleVersionId:'r1',ruleName:'Ordinaria',algorithmVersion:'s5.v1'},corrections:[{id:'x1',status:'approved',reason:'Entrada corregida',sourceEventId:'e1',effect:{id:'fx1',type:'clock_in',occurredAt:'2026-09-01T07:05:00.000Z'}}]}]};

describe('exports S9',()=>{it('produce CSV UTF-8 con evidencia y hash estable',()=>{const bytes=renderCsv(snapshot);const text=bytes.toString('utf8');expect(text.startsWith('\uFEFF')).toBe(true);expect(text).toContain('Ana Prueba');expect(text).toContain('approved e1');expect(integrityHash(bytes)).toBe(integrityHash(renderCsv(snapshot)));});it('produce PDF legible con cabecera válida',()=>{const pdf=renderPdf(snapshot);expect(pdf.subarray(0,8).toString()).toBe('%PDF-1.4');expect(pdf.toString('latin1')).toContain('Registro de jornada');});});
